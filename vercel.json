{
    "version": 2,
    "builds": [
        {
            "src": "api/index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/index.js"
        }
    ],
    "buildCommand": "npm install --prefix api && npm install --prefix web --legacy-peer-deps && npm run build --prefix web"
}